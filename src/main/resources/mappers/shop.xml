<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.repository.ShopRepository">
<select
        id="getCollectionList"
        parameterType="hashmap"
        resultType="com.demo.domain.CollectionProduct"
>
    SELECT
        pm.id,
        pm.category,
        pm.group_id,
        pm.name,
        pm.price,
        pig.temp_name,
        pc.total_count
    FROM
        product_mst pm
        LEFT OUTER JOIN product_img pig ON(pig.product_id = pm.id)
        LEFT OUTER JOIN (select
                                COUNT(*) AS total_count
                            FROM
                                (select
                                    *
                                from
                                    product_mst
                                WHERE
                                    1 = 1
                                    <if test='category != "all"'>
                                        and category = #{category}
                                    </if>
                                GROUP BY
                                    group_id) cnt) pc ON(1 = 1)
    WHERE
        1 = 1
        <if test='category != "all"'>
            and pm.category = #{category}
        </if>
    GROUP BY
        pm.group_id
    LIMIT 1, 8

</select>

    <select id="getProduct" parameterType="Integer" resultType="">
        SELECT
            pm.group_id,
            pm.name,
            pm.price,
            pm.color,
            pm.size,
            pim.temp_name
        FROM
            product_mst pm
            LEFT OUTER JOIN product_img pim ON(pim.product_id = (SELECT
                                                                    id
                                                                from
                                                                    product_mst
                                                                where
                                                                    group_id = #{group_id}
                                                                LIMIT 1))
        WHERE
            group_id = #{group_id}
    </select>


</mapper>